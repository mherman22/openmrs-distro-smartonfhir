# The SMART on FHIR demonstration stack: the O3 reference application, Keycloak 26 as its authorization
# server, and a SMART application launched from a patient chart.
#
#   cp .env.example .env     # set SMART_LAUNCH_SECRET
#   docker compose up -d --build

name: smartonfhir

services:
  gateway:
    image: openmrs/openmrs-reference-application-3-gateway:${TAG:-qa}
    restart: unless-stopped
    depends_on:
      - frontend
      - backend
    ports:
      - "${OPENMRS_PORT:-80}:80"

  frontend:
    # Built, not pulled: the app shell has to be assembled with the SMART launch frontend module in it.
    build:
      context: ./frontend
    restart: unless-stopped
    environment:
      SPA_PATH: /openmrs/spa
      API_URL: /openmrs
      SPA_CONFIG_URLS: /openmrs/spa/config-core_demo.json
      SPA_DEFAULT_LOCALE:
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      timeout: 5s
    depends_on:
      - backend

  backend:
    # The reference application image, unmodified: everything here is an environment variable or a mount.
    image: openmrs/openmrs-reference-application-3-backend:${TAG:-qa}
    restart: unless-stopped
    depends_on:
      - db
    environment:
      OMRS_CONFIG_MODULE_WEB_ADMIN: "true"
      OMRS_CONFIG_AUTO_UPDATE_DATABASE: "true"
      OMRS_CONFIG_CREATE_TABLES: "true"
      OMRS_CONFIG_CONNECTION_SERVER: db
      OMRS_CONFIG_CONNECTION_DATABASE: openmrs
      OMRS_CONFIG_CONNECTION_USERNAME: ${OMRS_DB_USER:-openmrs}
      OMRS_CONFIG_CONNECTION_PASSWORD: ${OMRS_DB_PASSWORD:-openmrs}
      # Change these together with the matching values in .env; Keycloak's realm reads those.
      OMRS_EXTRA_SMART_ISSUER: ${KEYCLOAK_PUBLIC_URL:-http://localhost:8180}/realms/openmrs
      OMRS_EXTRA_SMART_AUDIENCE: ${FHIR_BASE_URL:-http://localhost/openmrs/ws/fhir2/R4}
      # Keys are fetched container-to-container; apps read the discovery document from a browser.
      OMRS_EXTRA_SMART_JWKS_URI: http://keycloak:8080/realms/openmrs/protocol/openid-connect/certs
      OMRS_EXTRA_SMART_ADVERTISED_JWKS_URI: ${KEYCLOAK_PUBLIC_URL:-http://localhost:8180}/realms/openmrs/protocol/openid-connect/certs
      # The module's own defaults, stated so this file says what the deployment expects.
      OMRS_EXTRA_SMART_USERNAME_CLAIM: preferred_username
      OMRS_EXTRA_SMART_ALLOWED_CLOCK_SKEW_SECONDS: 10
      # A secret, so it stays out of the repository: OMRS_EXTRA_<NAME> arrives as smart.launch.secret.
      OMRS_EXTRA_SMART_LAUNCH_SECRET: ${SMART_LAUNCH_SECRET:?set SMART_LAUNCH_SECRET in .env}
      # Launchable apps are registered through the REST resource, not here; see docs/adding-a-smart-app.md.
      # Registers the bearer scheme; anything that is not a SMART credential goes to username/password.
      OMRS_EXTRA_AUTHENTICATION_SCHEME: smartbearer
      OMRS_EXTRA_AUTHENTICATION_SCHEME_SMARTBEARER_TYPE: org.openmrs.module.smartonfhir.web.smart.SmartBearerTokenAuthenticationScheme
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/openmrs"]
      timeout: 5s
    volumes:
      - openmrs-data:/openmrs/data
      # Shipped so a clone starts without a build; see ARTIFACTS.md. SMARTONFHIR_OMOD points at your own.
      - ${SMARTONFHIR_OMOD:-./backend/modules/smartonfhir.omod}:/openmrs/distribution/openmrs_modules/smartonfhir.omod:ro

  keycloak:
    # Keycloak plus the federation provider's JDBC driver; see keycloak/Dockerfile.
    build:
      context: ./keycloak
      args:
        KEYCLOAK_TAG: ${KEYCLOAK_TAG:-26.7.1}
    restart: unless-stopped
    # Pinned deliberately: every SPI the SMART provider implements is internal to Keycloak.
    command: [ start-dev, --import-realm ]
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KEYCLOAK_ADMIN:-admin}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:-admin}
      KC_HEALTH_ENABLED: "true"
      # Keycloak substitutes ${NAME} in an imported realm, so the realm is committed as a template.
      OPENMRS_BASE_URL: ${OPENMRS_BASE_URL:-http://localhost/openmrs}
      FHIR_BASE_URL: ${FHIR_BASE_URL:-http://localhost/openmrs/ws/fhir2/R4}
      SMART_APP_BASE_URL: ${SMART_APP_BASE_URL:-http://localhost:3000}
      SMART_LAUNCH_SECRET: ${SMART_LAUNCH_SECRET:?set SMART_LAUNCH_SECRET in .env}
      SSL_REQUIRED: ${SSL_REQUIRED:-none}
      OPENMRS_JDBC_URL: jdbc:mysql://db:3306/openmrs
      OPENMRS_DB_USER: ${OMRS_DB_USER:-openmrs}
      OPENMRS_DB_PASSWORD: ${OMRS_DB_PASSWORD:-openmrs}
    ports:
      - "${KEYCLOAK_PORT:-8180}:8080"
    volumes:
      # Shipped in this repository; see ARTIFACTS.md. Override with SMART_AUTH_JAR to run your own build.
      - ${SMART_AUTH_JAR:-./keycloak/providers/keycloak-smart-auth.jar}:/opt/keycloak/providers/openmrs-smart-auth.jar:ro
      # Optional: lets clinicians sign in with the OpenMRS password they already have.
      - ${USERSTORE_JAR:-./keycloak/providers/openmrs-keycloak-userstore.jar}:/opt/keycloak/providers/openmrs-userstore.jar:ro
      - ./keycloak/realm:/opt/keycloak/data/import:ro
      # Keycloak state is deliberately not persisted: the realm is re-imported on every start.
    depends_on:
      # Waits for OpenMRS, not just the database: the federation provider validates against its schema.
      backend:
        condition: service_healthy
    healthcheck:
      test: [ "CMD-SHELL", "exec 3<>/dev/tcp/127.0.0.1/9000 && echo -e 'GET /health/ready HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n' >&3 && cat <&3 | grep -q '\"status\": \"UP\"'" ]
      interval: 5s
      timeout: 5s
      retries: 40
      start_period: 20s

  smart-app:
    # Released on its own and pulled as an image, because that is what a SMART application is:
    # https://github.com/mherman22/openmrs-smart-vitals-reviews-app
    image: ghcr.io/mherman22/openmrs-smart-vitals-reviews-app:${SMART_APP_VERSION:-0.1.4}
    restart: unless-stopped
    ports:
      - "${SMART_APP_PORT:-3000}:8080"
    environment:
      SMART_CLIENT_ID: smartClient
      # Enables the standalone launch as well as the EHR one.
      SMART_ISS: ${FHIR_BASE_URL:-http://localhost/openmrs/ws/fhir2/R4}
      SMART_ORGANIZATION: OpenMRS Reference Application
      # Lets OpenMRS embed the app in an iframe; the EHR's origin has to be named, 'self' is not enough.
      SMART_FRAME_ANCESTORS: "'self' ${OPENMRS_FRAME_ORIGIN:-http://localhost}"

  db:
    image: mariadb:10.11.7
    restart: unless-stopped
    command: "mysqld --character-set-server=utf8mb4 --collation-server=utf8mb4_general_ci"
    healthcheck:
      test: "mysql --user=${OMRS_DB_USER:-openmrs} --password=${OMRS_DB_PASSWORD:-openmrs} --execute \"SHOW DATABASES;\""
      interval: 3s
      timeout: 1s
      retries: 5
    environment:
      MYSQL_DATABASE: openmrs
      MYSQL_USER: ${OMRS_DB_USER:-openmrs}
      MYSQL_PASSWORD: ${OMRS_DB_PASSWORD:-openmrs}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-openmrs}
    volumes:
      - db-data:/var/lib/mysql

volumes:
  openmrs-data: ~
  db-data: ~
